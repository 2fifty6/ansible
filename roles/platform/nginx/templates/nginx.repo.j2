[nginx-{{ nginx_version }}]
name=nginx repo (v{{ nginx_version }})
baseurl=http://nginx.org/packages/{{ ansible_distribution.lower().replace('redhat','rhel') }}/{{ ansible_distribution_major_version }}/{{ ansible_architecture }}/
gpgcheck=0
enabled=1
